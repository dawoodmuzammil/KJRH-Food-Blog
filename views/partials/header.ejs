<!DOCTYPE html>
<html>
    <head> 
        <title>YelpCamp</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.1.0/css/all.css" 
            integrity="sha384-lKuwvrZot6UHsBSfcMvOkWwlCMgc0TaWr+30HWe3a4ltaBwTZhyTEggF5tJv8tbt" crossorigin="anonymous">
        <link href="https://fonts.googleapis.com/css?family=Hanalei+Fill" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css?family=Alfa+Slab+One|Raleway+Dots|Sawarabi+Mincho|Sura" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css?family=Quicksand|Source+Sans+Pro:700,900,900i|Source+Serif+Pro:400,700" rel="stylesheet">




</head>
    <body>
        <nav class="navbar navbar-default">
            <div class="container-fluid container">
                <div class="navbar-header">
                    <a class="navbar-brand" href="/" style="font-family: 'Hanalei Fill', cursive; font-size: 1.6em">YelpCamp</a>
                </div>
                <ul class="nav navbar-nav">
                    <li><a href="/restaurants"><i class="fas fa-home"></i>   Home</a></li>
                </ul>
                <div class="collapse navbar-collapse">
                    <ul class="navbar-nav navbar-right nav">
                        <!--if no user-->
                        <% if ( !currentUser) { %>
                            <li><a href="/login">Login   <i class="fas fa-user"></i></a></li>
                            <li><a href="/register">Sign Up   <i class="fas fa-user-plus"></i></a></li>
                        <!--else-->
                        <% } else {%>
                            <li><a style="color: #424344;" href="#"><strong>Currently signed in as: </strong><%= currentUser.username %></a></li>
                            <li><a href="/logout">Logout</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>
        
        <div class="container">
            <% if ( error && error.length > 0) { %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %> 
            <% if ( success && success.length > 0) { %>   
                <div class="alert alert-success" role="alert">
                    <%= success %>
                </div>
            <% } %>
        </div>
        